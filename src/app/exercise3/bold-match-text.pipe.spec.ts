import { BoldMatchTextPipe } from './bold-match-text.pipe';

describe('BoldMatchTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldMatchTextPipe();
    expect(pipe).toBeTruthy();
  });
});

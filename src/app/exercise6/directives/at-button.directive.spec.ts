import { AtButtonDirective } from './at-button.directive';

describe('AtButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AtButtonDirective();
    expect(directive).toBeTruthy();
  });
});
